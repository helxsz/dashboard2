/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PagingPipe } from './paging.pipe';

describe('PagingPipe', () => {
  it('create an instance', () => {
    let pipe = new PagingPipe();
    expect(pipe).toBeTruthy();
  });
});
